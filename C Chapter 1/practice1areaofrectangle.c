#include <stdio.h>

int main()
{
	int length,breath;
	int area=length*breath;

	printf("Entre the value of length is\n",area);
	scanf("%d",&length);



	printf("Entre the value of breath is\n",area);
	scanf("%d",&breath);



	printf("the area of the rectangle is %d", length*breath );
	return 0;
}